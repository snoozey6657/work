{
  "name": "winco-leads",
  "version": "1.0.0",
  "private": true,
  "description": "Winnebago County contractor lead aggregator",
  "scripts": {
    "build": "cd frontend && npm install && CI=false GENERATE_SOURCEMAP=false npm run build",
    "start": "node backend/server.js",
    "install-all": "cd frontend && npm install && cd ../backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
